---
name: account-abstracter
description: 给我一个账号，还你一个灵魂。从公开作品中逆向工程创作者的写作DNA，封装为可复用的 .skill 文件，让任何 Agent 都能复刻目标账号的文风、节奏和叙事逻辑。
---

# Account Abstracter

> **给我一个账号，还你一个灵魂。**

从任何新媒体账号的公开作品中逆向工程创作者的写作 DNA——文风、结构、金句、节奏、情感弧线——然后封装成一份 `.skill` 文件，让 Claude / Open Claw / WorkBuddy 等任何 Agent 平台都能瞬间获得该账号的创作能力。

这不是简单的风格模仿。这是一套四层逆向工程体系：检索定位 → 文本解剖 → 画像合成 → Skill 封装，每一步都有置信度追踪，每一条结论都有作品佐证。最后还有回测验证——用生成的 Skill 产出内容，与原作逐维对比，不达标就回炉。

**一个账号进，一个 .skill 出。即插即用，开箱即写。**

## 核心工作流

```
用户输入（账号名 + 作品文本）
        ↓
  ┌─────────────┐    ┌─────────────────┐
  │  Layer 1    │    │   Layer 2       │
  │  检索层     │    │   文本分析层    │
  │  浏览器搜索 │    │   作品解析      │
  └──────┬──────┘    └────────┬────────┘
         │           [身份确认]│
         └────────┬───────────┘
                  ↓
           整合数据池（文本优先，检索补充）
                  ↓
          ┌───────────────┐
          │   Layer 3     │
          │   分析层      │
          └───────┬───────┘
                  ↓
          [用户确认画像]
                  ↓
          ┌───────────────┐
          │   Layer 4     │
          │   Skill封装层 │
          └───────┬───────┘
                  ↓
          [回测验证] → 通过 → <账号名>.skill
```

---

## Layer 1 — 检索层

### 触发条件

用户提供了账号名（即使未提供作品文本也可先行检索）。

### 执行步骤

1. **平台勾选**：向用户展示平台选项，勾选需要检索的平台
   - 可选平台：微信公众号 / 微博 / 小红书 / 知乎 / B站 / 抖音 / Twitter-X / 其他
   - 默认全选，用户可取消不需要的平台

2. **按策略检索**：根据勾选平台，参考 `references/platform-search-strategy.md` 中的搜索策略执行检索

3. **账号身份确认**（关键节点，不可跳过）：
   - 展示搜到的账号信息：名称、简介摘要、粉丝量、近期3条内容标题
   - 用户确认："是这个号" / "不是，重新搜索" / "跳过检索，仅走文本分析"
   - 若搜到多个疑似账号，全部展示让用户选择

4. **信息提取**：从确认的账号页面提取以下信息
   - 账号简介、定位标签
   - 粉丝量级、内容频率
   - 近期热门内容标题/摘要
   - 外部评价（转发评论中的关键词）

5. **失败兜底**：若检索失败或信息极少，降级为仅走 Layer 2，并标注"检索数据不足"

### 输出

结构化账号基础档案（JSON），格式参考 `references/output-schemas.md`

---

## Layer 2 — 文本分析层

### 触发条件

用户提供了作品文本文件（1个或多个）。

### 接受与拒绝格式

- **接受**：.txt / .md / .docx / .csv 及其他纯文本格式
- **拒绝**：图片、视频、音频——遇到时提示用户仅接受文本类文件

### 输入量评估

| 作品数量 | 质量等级 | 处理策略 |
|----------|----------|----------|
| 1-2篇 | ⚠️ 粗略 | 执行基础分析，强提醒用户结果偏差大 |
| 3-4篇 | 🟡 基础 | 正常分析，部分维度标注低置信度 |
| 5-10篇 | 🟢 推荐 | 各维度分析可靠 |
| 10+篇且跨度≥3个月 | 🔵 深度 | 启用风格演进追踪 |

### 分析维度

**基础维度：**
- 标题规律（字数、句式、情绪词）
- 开头/结尾模式（钩子类型、收尾习惯）
- 段落结构（长短句比例、分段逻辑）
- 高频词汇与表达
- 数字/数据引用习惯
- 案例/故事使用频率

**增强维度：**
- 内容主题聚类（核心话题域、常驻话题 vs 偶发话题）
- 互动/CTA模式（引导互动的套路、出现频率与位置）
- 发布节奏与篇幅规律（典型篇幅范围、更新频率）
- 情感弧线（单篇情感走势类型）
- 视觉排版习惯（分段密度、emoji使用、分隔符偏好）

各维度详细分析指南参考 `references/analysis-dimensions.md`

### 输出

文本特征向量（可读报告 + 结构化数据），各维度附置信度标签（高/中/低）

---

## Layer 3 — 分析层

### 数据对齐规则

Layer 1 和 Layer 2 数据整合时：
1. **文本分析优先**：作品是第一手数据，当检索信息与文本分析矛盾时以文本结论为准
2. **检索信息补充**：用于填充文本无法提取的信息（粉丝画像、外部评价等）
3. **矛盾标记**：显著矛盾时在画像中明确标注，由用户在确认环节裁决

### 综合画像生成

生成包含以下维度的综合画像，每个维度附置信度：

| 维度 | 内容 |
|------|------|
| 用户定位 | 受众群体、内容赛道、差异化标签 |
| 文风特征 | 语气、节奏、情感倾向 |
| 创作结构 | 常用框架（问题驱动/故事型/干货列表等） |
| 素材引用 | 数据来源偏好、案例类型、引用密度 |
| 金句架构 | 修辞手法、情感触发点、句式模板 |
| 选题规律 | 话题偏好、热点响应模式 |
| 内容主题域 | 核心话题聚类 |
| 互动模式 | CTA类型、位置、频率 |
| 篇幅节奏 | 典型篇幅范围、发布频率 |
| 情感弧线 | 常用情感走势类型 |
| 排版风格 | 分段密度、emoji习惯、视觉分隔方式 |

### 风格演进追踪（可选）

当作品数量≥10篇且时间跨度≥3个月时启用：
- 创作风格随时间的演变趋势
- 当前阶段特征（而非全量平均）

### 用户确认（必须）

将完整画像展示给用户，用户可：
- 修改任何维度的结论
- 标记"不准"让系统重新分析
- 确认后才能进入 Layer 4

---

## Layer 4 — Skill 封装层

### .skill 文件结构

```
<账号名>.skill
├── 元数据          # 账号名、平台、生成时间、版本、数据源说明
├── 声明            # 伦理声明（必须）
├── 角色设定        # 模拟该创作者的身份与定位
├── 写作规则        # 基于分析的文风约束
├── 结构模板        # 2-3个常用创作框架
├── 词汇库          # 高频词、禁用词、金句模式
├── 互动模板        # CTA句式库
├── Few-shot示例    # 3-5条典型作品片段
├── 使用说明        # 擅长场景、失真场景、建议输入格式
└── 置信度报告      # 各维度分析置信度
```

封装模板参考 `references/skill-template.md`

### 伦理声明（每个 .skill 必须包含）

```
本 skill 基于 [账号名] 的公开作品分析生成，仅供学习研究参考。
不代表原作者立场，不得用于冒充原作者发布内容。
```

### 回测验证

封装完成后执行：
1. 使用生成的 skill 产出1-2篇内容
2. 与原作者真实作品对比（文风/结构/用词/情感）
3. 输出相似度评分
4. 评分不足时提示用户，建议回退调整

### 输出路径

生成的 `.skill` 文件默认存储到本 skill 目录下的 `soul/` 子文件夹中：
```
/Users/wushaojian/Desktop/renaissance/account-abstracter/soul/<账号名>.skill
```
若 `soul/` 目录不存在则自动创建。

### 兼容目标

Claude / Open Claw / WorkBuddy 等支持 skill 注入的 agent 平台

---

## 关键设计决策

1. Layer 1 和 Layer 2 并行执行，不互相阻塞
2. 文本分析层对作品数量有最低要求（建议 ≥5 篇），不足时提示并标注低置信度
3. Layer 3 分析结果必须经用户确认后才进入封装
4. .skill 文件格式采用 Markdown+frontmatter，保证可读性和可编辑性
5. 检索层必须经过账号身份确认
6. 数据对齐原则：文本分析优先，检索信息补充
7. 每个分析维度附带置信度标签
8. 封装后执行回测验证
9. 所有 .skill 文件必须包含伦理声明

## Resources

### references/

详细参考文档，按需加载：

- `platform-search-strategy.md` — 各平台搜索策略映射、反爬应对、登录要求
- `analysis-dimensions.md` — 各分析维度的详细分析指南、示例、判断标准
- `skill-template.md` — .skill 文件的完整封装模板、各字段填写规范
- `output-schemas.md` — 各层输出的 JSON Schema 定义

### scripts/

- `validate_skill.py` — 验证生成的 .skill 文件结构完整性和格式正确性
